# Dev: Vite proxies /api to this URL (see vite.config.ts)
VITE_PROXY_TARGET=http://127.0.0.1:8000

# Optional: absolute API base for production builds (empty = same origin)
# VITE_API_URL=

# Optional dev server port
# VITE_DEV_PORT=5173
